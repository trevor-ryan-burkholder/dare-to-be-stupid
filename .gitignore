node_modules/
*.tsbuildinfo

# Run state for a `meeseeks` run, never this repo's own (see CLAUDE.md scope note).
.meeseeks/

# Left behind by the schemathesis gate when its argv was verified. A tool cache, never ours.
.hypothesis/
dare-to-be-stupid/
